name: auth_service
description: Flutter Package that provides complete code for email authentication using Firebase
version: 0.0.1

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter:

dependencies:
  equatable:
  firebase_auth:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints:
